using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProvisionaSpawnEnemigoParaPruebas : MonoBehaviour {

    public GameObject prefabEnemigo;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    public void instanciarEnemigo()
    {
        //Instantiate(prefabEnemigo, new Vector3(17.42f, -3.03f, 11), Quaternion.identity, this.gameObject.transform.parent.transform);
        //Instantiate(prefabEnemigo, new Vector3(17.42f, 7.47f, -1.26f), Quaternion.identity, this.gameObject.transform.parent.transform);

    }
}
